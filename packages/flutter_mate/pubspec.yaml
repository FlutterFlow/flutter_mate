name: flutter_mate
description: Automation SDK for Flutter apps - enables in-app AI agents and external control
version: 0.1.0
repository: https://github.com/user/flutter_mate

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_mate_types:
    path: ../flutter_mate_types

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
